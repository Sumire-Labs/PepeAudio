# Local-only integration test ports. Do not use this override on a public host.
services:
  postgres:
    ports:
      - 127.0.0.1:55432:5432
    networks:
      - data
      - test_edge

  valkey:
    ports:
      - 127.0.0.1:56379:6379
    networks:
      - data
      - test_edge

networks:
  test_edge:
