# The frontend is a standalone package, independent of the bot's pnpm workspace.
# Neutralize the root workspace's supply-chain release-age gate here and don't
# re-verify deps on every `pnpm run` (it would re-trigger the root policy).
minimum-release-age=0
verify-deps-before-run=false
